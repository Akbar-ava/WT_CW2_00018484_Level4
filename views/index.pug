<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Goal Tracker</title>
    <style>
        :root {
            --dark-teal: #2b647d;
            --light-blue: #6cb4d1;
            --off-white: #f5f5f5;
            --orange: #e86f40;
            --brown: #8b5238;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--off-white);
            color: #333;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }
        
        header {
            background-color: var(--dark-teal);
            color: white;
            padding: 1rem;
            text-align: center;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .goals-container {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        
        h1, h2 {
            color: var(--dark-teal);
        }
        
        .btn {
            padding: 10px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.2s;
        }
        
        .btn-create {
            background-color: var(--light-blue);
            color: white;
        }
        
        .btn-create:hover {
            background-color: #5a9cb8;
        }
        
        .btn-update {
            background-color: var(--orange);
            color: white;
        }
        
        .btn-update:hover {
            background-color: #d65f32;
        }
        
        .btn-delete {
            background-color: var(--brown);
            color: white;
        }
        
        .btn-delete:hover {
            background-color: #7a4630;
        }
        
        .form-group {
            margin-bottom: 1rem;
        }
        
        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
            color: var(--dark-teal);
        }
        
        input, select, textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
            margin-bottom: 0.5rem;
        }
        
        .goal-item {
            background-color: var(--off-white);
            border-left: 4px solid var(--light-blue);
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 4px;
        }
        
        .goal-actions {
            display: flex;
            gap: 8px;
            margin-top: 0.5rem;
        }
        
        .goal-progress {
            margin-top: 0.5rem;
            background-color: #e0e0e0;
            border-radius: 4px;
            height: 8px;
            overflow: hidden;
        }
        
        .progress-bar {
            background-color: var(--light-blue);
            height: 100%;
        }
    </style>
</head>
<body>
    <header>
        <h1>Fitness Goal Tracker</h1>
    </header>
    
    <div class="container">
        <div class="goals-container">
            <h2>Create New Goal</h2>
            <form id="goalForm">
                <div class="form-group">
                    <label for="goalTitle">Goal Title</label>
                    <input type="text" id="goalTitle" placeholder="E.g., Run 5K, Lose 10 pounds" required>
                </div>
                
                <div class="form-group">
                    <label for="goalType">Goal Type</label>
                    <select id="goalType">
                        <option value="cardio">Cardio</option>
                        <option value="strength">Strength</option>
                        <option value="flexibility">Flexibility</option>
                        <option value="nutrition">Nutrition</option>
                        <option value="weight">Weight</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <label for="targetDate">Target Date</label>
                    <input type="date" id="targetDate" required>
                </div>
                
                <div class="form-group">
                    <label for="goalDescription">Description (Optional)</label>
                    <textarea id="goalDescription" rows="3" placeholder="Add more details about your goal"></textarea>
                </div>
                
                <button type="submit" class="btn btn-create">Create Goal</button>
            </form>
        </div>
        
        <div class="goals-container">
            <h2>My Fitness Goals</h2>
            <div id="goalsList">
                <!-- Sample goal for demonstration -->
                <div class="goal-item">
                    <h3>Run 5K under 30 minutes</h3>
                    <p><strong>Type:</strong> Cardio</p>
                    <p><strong>Target Date:</strong> June 1, 2025</p>
                    <p>Current best time is 35 minutes. Need to improve by 5 minutes.</p>
                    
                    <div class="goal-progress">
                        <div class="progress-bar" style="width: 60%"></div>
                    </div>
                    
                    <div class="goal-actions">
                        <button class="btn btn-update">Update</button>
                        <button class="btn btn-delete">Delete</button>
                    </div>
                </div>
                
                <div class="goal-item">
                    <h3>Bench Press 200 lbs</h3>
                    <p><strong>Type:</strong> Strength</p>
                    <p><strong>Target Date:</strong> July 15, 2025</p>
                    <p>Current max is 165 lbs. Increase by 5 lbs every two weeks.</p>
                    
                    <div class="goal-progress">
                        <div class="progress-bar" style="width: 40%"></div>
                    </div>
                    
                    <div class="goal-actions">
                        <button class="btn btn-update">Update</button>
                        <button class="btn btn-delete">Delete</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Sample JavaScript functionality
        document.getElementById('goalForm').addEventListener('submit', function(e) {
            e.preventDefault();
            alert('Goal creation functionality would be implemented here!');
            // In a real application, you would add the goal to storage and update the display
        });
        
        // Add event listeners to update and delete buttons
        document.querySelectorAll('.btn-update').forEach(button => {
            button.addEventListener('click', function() {
                alert('Update goal functionality would be implemented here!');
            });
        });
        
        document.querySelectorAll('.btn-delete').forEach(button => {
            button.addEventListener('click', function() {
                alert('Delete goal functionality would be implemented here!');
            });
        });
    </script>
</body>
</html>